@model List<Product>
@{
    ViewData["Title"] = "Index";
}
<!-- PRODUCTS START -->
<section id="products">
    <div class="container">
        <div class="row parent">
            @await Component.InvokeAsync("Product")
            @*@foreach (var product in Model)
                {
                    <div class="col-sm-6 col-md-4 col-lg-3 mt-3">
                        <div class="product-item text-center">
                            <div class="img">
                                <a href="">
                                    <img src="~/img/@product.Image" class="img-fluid" alt="">
                                </a>
                            </div>
                            <div class="title mt-3">
                                <h6>@product.Title</h6>
                            </div>
                            <div class="price">
                                <span class="text-black-50">Add to cart</span>
                                <span class="text-black-50">$@product.Price</span>
                            </div>
                        </div>
                    </div>
                }
        </div>*@
        <input type="hidden" id="ProductCount" value="@ViewBag.ProductCount" />
        <div class="row justify-content-center me-5">
            <button class="btn btn-warning" id="loadMore">Load More...</button>
        </div>
    </div>
</section>

<!-- PRODUCTS END -->
